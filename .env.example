# ─────────────────────────────────────────────────────────────
# Trip OS 前端 · .env.local（仅用于本地开发配置）
# ─────────────────────────────────────────────────────────────────────────────────
# 说明：v2 之后，AI / 高德的 key 都不在前端了，浏览器也拿不到。
# 这些 key 都在 server/.env 里，由后端代为转发请求。
#
# 唯一可能需要在这里配的，是高德 Web 端 (JS API) Key（给浏览器显示地图用）。
# 没接入地图之前，整个 .env.local 可以为空文件甚至不需要。
# ─────────────────────────────────────────────────────────────

# 高德地图 Web 端 JS API Key（控制台选「Web 端 (JS API)」平台时拿到的 Key）
# 没配置也能跑（Mock 地图），配置后会自动切换到真实底图
VITE_AMAP_JS_KEY=